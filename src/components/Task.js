import React from "react";

const Task = () => {
  return (
    <div
      style={{
        backgroundColor: "#FFFFFF",
        borderRadius: "9999px",
        padding: "10px",
      }}
    >
      Check box + To do list + Options
    </div>
  );
};

export default Task;
